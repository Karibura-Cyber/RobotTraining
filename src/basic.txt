#include <POP32.h>

void setup(){
    /*
        FD(speed); // คำสั่งเดินหน้า 4 ล้อ

        BK(speed); // คำสั่งถอยหลัง 4 ล้อ

        SL(speed); // คำสั่งหมุนซ้าย 4 ล้อ

        SR(speed); // คำสั่งหมุนขวา 4 ล้อ

        TL(speed); // คำสั่งหมุนซ้าย 2 ล้อ

        TR(speed); // คำสั่งหมุนขวา 2 ล้อ

        FD2(speed1, speed2); // คำสั่งเดินหน้าแบบ คู่

        BK2(speed1, speed2); // คำสั่งเดินหน้าแบบ คู่

        AO(); // คำสั่งหยุด
        sleep(duration); // กำหนดระยะเวลาการทำงาน 
    */
}

void loop(){
    FD(50); sleep(1000);    // เดินหน้าด้วยความเร็ว 50% เป็นเวลา 1 วินาที
    AO(); sleep(1000);      // หยุดเป็นเวลา 1 วินาที
}